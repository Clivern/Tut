# Server configs
app:
  # Server name
  name: ${TUT_SERVER_NAME:-tut}
  # Env mode (dev or prod)
  mode: ${TUT_SERVER_MODE:-prod}
  # HTTP port
  port: ${TUT_SERVER_PORT:-8000}
  # Hostname
  hostname: ${TUT_SERVER_HOSTNAME:-127.0.0.1}
  # TLS configs
  tls:
    status: ${TUT_SERVER_TLS_STATUS:-off}
    crt_path: ${TUT_SERVER_TLS_PEMPATH:-cert/server.crt}
    key_path: ${TUT_SERVER_TLS_KEYPATH:-cert/server.key}

  # Global timeout
  timeout: ${TUT_SERVER_TIMEOUT:-50}

  # Prometheus metrics endpoint
  metrics:
    username: ${TUT_SERVER_PROM_METRICS_USERNAME:-admin}
    secret: ${TUT_SERVER_PROM_METRICS_SECRET:-secret}

  # Log configs
  log:
    # Log level, it can be debug, info, warn, error, panic, fatal
    level: ${TUT_SERVER_LOG_LEVEL:-debug}
    # Output can be stdout or abs path to log file /var/logs/tut.log
    output: ${TUT_SERVER_LOG_OUTPUT:-stdout}
    # Format can be json
    format: ${TUT_SERVER_LOG_FORMAT:-json}

  # Database configs
  database:
    # Database driver (postgres, postgresql, or sqlite)
    driver: ${TUT_DATABASE_DRIVER:-sqlite}
    # PostgreSQL specific configs
    host: ${TUT_DATABASE_HOST:-localhost}
    port: ${TUT_DATABASE_PORT:-5432}
    username: ${TUT_DATABASE_USERNAME:-postgres}
    password: ${TUT_DATABASE_PASSWORD:-postgres}
    name: ${TUT_DATABASE_NAME:-tut}
    # Connection pool settings
    max_open_conns: ${TUT_DATABASE_MAX_OPEN_CONNS:-25}
    max_idle_conns: ${TUT_DATABASE_MAX_IDLE_CONNS:-10}
    conn_max_lifetime: ${TUT_DATABASE_CONN_MAX_LIFETIME:-300}
    # SQLite specific config (path to database file)
    datasource: ${TUT_DATABASE_DATASOURCE:-./cache/tut.db}
